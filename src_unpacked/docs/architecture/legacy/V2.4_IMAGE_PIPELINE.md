# v2.4 Image Pipeline
Windows uses WIC (Windows Imaging Component) to decode an image, scale it to
64x64, convert it to 8-bit grayscale, and feed those pixels into the existing
perceptual hash engine. The decoder is isolated behind ImageDecoder, leaving
room for other backends later.
