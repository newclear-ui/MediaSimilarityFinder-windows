# v3.1 Incremental Index
Tracks path, file size and modification timestamp and classifies the next scan
as Unchanged, Added, Modified or Removed. Unchanged entries can skip expensive
fingerprinting; changed entries are candidates for re-analysis. The storage
adapter remains separate so it can later persist to SQLite or another DB.
