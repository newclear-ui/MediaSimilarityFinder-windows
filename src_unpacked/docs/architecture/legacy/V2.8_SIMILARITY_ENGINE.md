# v2.8 Similarity Engine
Adds reusable perceptual-hash comparison primitives. The engine returns
similarity as a percentage and can find the best matching frame between two
fingerprints. This is the comparison layer for later candidate grouping and
CUDA batch acceleration.
