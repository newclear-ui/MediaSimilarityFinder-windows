# v2.9 Candidate Index
First-stage perceptual-hash candidate filtering using Hamming distance.
The API is isolated so the implementation can later be replaced by a
BK-tree, hash buckets, parallel search, or CUDA without changing callers.
