# v3.13-v3.17 feature bundle

- v3.13: 64-bit DCT perceptual hash (pHash) replaces image/video average hash in the CPU analysis path.
- v3.14: temporal sliding alignment for video similarity, allowing clips with shifted start positions.
- v3.15: CUDA batch accelerator boundary with real device allocation/copy/kernel/synchronization and CPU fallback.
- v3.16: GPU detection and batch path are isolated behind GpuBackend for later CUDA pHash/NVDEC work.
- v3.17: integrated quality/performance regression tests and release packaging.
