# MediaSimilarityFinder 0.9.2.7 Portable

Windows x64 portable packaging. Index data remains application-owned under `Index/<folder-id>/`; scanned media folders are not used for index storage.

This build also improves local SQLite write behavior and keeps image/video candidate searches separated.
