# CUDA 0.9.2.9

The 0.9.2.9 resident monitor continues to reuse the existing CUDA image pipeline. Windows filesystem events are handled without requiring CUDA/NVML linkage; optional `nvidia-smi` telemetry remains used for workload protection.
