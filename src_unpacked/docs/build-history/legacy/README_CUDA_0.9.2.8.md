# CUDA 0.9.2.8

The 0.9.2.8 monitor foundation reuses the existing CUDA image pipeline when GPU analysis is enabled. Runtime workload protection can defer analysis before GPU work starts. NVIDIA CUDA compilation/runtime still requires a Windows machine with the CUDA toolkit and a compatible display driver.
