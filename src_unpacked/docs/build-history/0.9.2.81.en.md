# MediaSimilarityFinder v0.9.2.81 — Single-Tree Revalidation + GUI Test Plugin Path

## Need for change
- After merging the `C:\project` / `C:\project-b64` worktrees, the stale build outputs in the canonical folder could not be kept.
- The preserved branch (`archive/project-wip-ql-revert`) was diffed for CI/packaging/docs/ffmpeg items, but everything had already converged into (or been validated on) the latest line, so nothing needed porting.
- The fresh tree built cleanly, yet 4 GUI tests crashed without a Qt platform plugin.

## Previous structure
- Qt platform plugins (`qoffscreen`, etc.) exist only under `vcpkg_installed/x64-windows/Qt6/plugins`, unreachable from the build output directory or `QT_PLUGIN_PATH`.
- GUI tests had no guaranteed plugin path under CTest.

## New structure
- `CMakeLists.txt` derives the plugin directory from the `Qt6::Core` location and sets `QT_PLUGIN_PATH` for all 8 GUI tests.
- No matching-logic or CUDA algorithm changes. Version strings unified → 0.9.2.81.

## Resolved situation
- Clean reconfigure + full build in the single folder (`C:\project`) succeeds, **CTest 56/56 PASS**.
- Preserved-branch review: CI already converged, `package_portable.ps1` and `storage-design.md` converged, the ffmpeg trim (`ffprobe`/`ffmpeg` only) is already live on this line with green video e2e tests, and the QuickLook revert was dropped per the keep-current-implementation decision (kept only on the archive branch).

## Validation
- Core+GUI Release build: PASS, **CTest 56/56 PASS**
- `--version`/`--smoke` exit 0

## Future impact
- GUI tests now get their plugin path from CMake, so fresh build directories reproduce green runs.
- `build-windows-cpu` was removed and not recreated; regenerate it via `build_windows_cpu.ps1` if a CPU-only revalidation is needed.
